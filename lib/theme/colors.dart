import "package:flutter/material.dart";

const PRIMARY_COLOR = Color(0xFFFFFFFF); // appbar에 적용되는 색
const SECONDARY_COLOR = Color(0xFFFAFAFA); // bottom navigation에 적용되는 색

const SURFACE_COLOR = Color(0xFFFFFFFF);
const ERROR_COLOR = Color(0xFFC5032B);
const BACKGROUND_COLOR = Color(0xFFFFFFFF);

const TEXT_COLOR = Color(0xFF000000);

const BOTTOM_NAV_BAR_BACKGROUND_COLOR = Color(0xFFFFFFFF);

const DIVIDER_COLOR = Color(0xFFC1C1C1);
